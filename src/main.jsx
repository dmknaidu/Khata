import React from 'react'
import ReactDOM from 'react-dom/client'
import ExpenseTracker from '../khata-expense-tracker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ExpenseTracker />
  </React.StrictMode>,
)
